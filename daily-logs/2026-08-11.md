# Daily Log — 2026-08-11

Let's Pilates LA · https://github.com/letspilates/Live

## 오늘 한 일 (요약)

Hero 설립연도 수정과 Instructors 섹션 자격 칩 보강. 컨텐츠 전용 변경으로 스테이징 검증 후 **프로덕션까지 배포 완료**.

---

## 1. 지난 작업 확인 (세션 시작)

- 마지막 작업일: **2026-07-23** (약 3주 전).
- `origin/Staging` == `origin/main` — 밀린 미배포 작업 **없음**.
- PR #32(웰컴 이메일 실제 정책 문구 + 관리자 로그인 + 소스 난독화)는 07-23 16:02 UTC에 사용자가 직접 병합, 배포 success.
  → **2026-07-23 로그에는 이 최종 승격이 누락돼 있어 오늘 보완 기록함** (해당 파일 하단 참조).

---

## 2. 변경 내용 (모두 컨텐츠 — 디자인 미변경)

변경 파일: `pilates-landing/src/i18n/translations.ts` **단 하나**. 칩 마크업·스타일·레이아웃은 손대지 않음.

### Hero
- 통계 항목 `Est. 2015` → **`Est. 2014`** (EN·KR 양쪽).
- 코드베이스 전체에서 다른 `2015` 참조 없음 확인 (index.html JSON-LD에도 foundingDate 없음).

### Instructors — 자격 칩
| 강사 | 추가 |
|---|---|
| **Sunnie Lee** | GYROKINESIS® · GYROTONIC® Archway · GYROTONIC® Jumping Stretching Board · GYROTONER® · Pre / Post Natal |
| Eunice Choi | Pre / Post Natal |
| Su Kyung Yi | Pre / Post Natal |
| Haley Han | Pre / Post Natal |

- KR에서는 `Pre / Post Natal` → **`산전 · 산후`**.
- **상표 표기 통일** (사용자 확인: "GYROTONIC® Archway 이게 맞는거야"):
  Eunice Choi의 `Gyrokinesis®` → `GYROKINESIS®`, 단독 `Archway` → `GYROTONIC® Archway`.
  이제 4개 카드 표기가 일치.

---

## 3. 검증 방법 (재현용 메모)

- 컨테이너가 새로 뜨면 `pilates-landing/node_modules`가 비어 있음 → **`npm ci` 먼저** 실행해야 lint/build 동작.
- `npm run lint` 통과, `npm run build` 성공.
- **CI와 동일한 빌드를 재현해 검증**: `npm run build -- --base=/staging/` → `_site/staging/` 구조로 복사 → 로컬 서빙 → Playwright(`/opt/pw-browsers/chromium`)로 렌더 검사.
  결과: Est. 2014 ✅ / Est. 2015 잔존 없음 ✅ / GYROTONER®·GYROTONIC® Archway 노출 ✅ / `Pre / Post Natal` 4개 ✅ / KR `산전 · 산후` 4개 ✅ / 구 단독 `Archway` 칩 제거 ✅.
- 데스크톱(1440)·모바일(390) 스크린샷 확인 — Sunnie 카드 7개 칩이 3줄로 정상 줄바꿈, 카드 높이 정렬 유지, 잘림 없음.
- 모바일 문서 가로 스크롤 8px은 **기존 marquee 티커** 때문 (이번 변경과 무관, 사전 존재).

### ⚠️ "스테이징에 안 나온다" 이슈 (해결됨)
사용자가 반영이 안 보인다고 해서 재확인 → **배포·빌드는 정상**이었음(위 검증 통과). 브라우저 캐시 또는 프로덕션 루트 주소를 본 것이 원인.
- 교훈: `https://letspilatesla.com/` 루트는 `main` 빌드라 스테이징 변경이 안 보이는 게 정상. 반드시 **`/staging/`** (끝 슬래시 포함) + 강력 새로고침(`Cmd/Ctrl+Shift+R`).
- 참고: 이 실행 환경의 프록시가 `letspilatesla.com` / `letspilates.github.io` 접속을 **정책 차단(403)** → Claude는 라이브 페이지를 직접 못 엶. 검증은 (a) origin 브랜치 파일 확인 + (b) CI 동일 빌드 로컬 재현 + (c) Actions 결과로 대체.

---

## 4. 배포 이력

| 단계 | 커밋 / PR | 결과 |
|---|---|---|
| Staging 1차 | `f9ebb66` Est. 2014 + Sunnie 칩 + 전원 Pre/Post Natal | Actions **success** (06:00 UTC) |
| Staging 2차 | `c28944d` 상표 표기 통일 | Actions **success** (06:03 UTC) |
| 사용자 스테이징 확인 | "잘나와" — 승인 | ✅ |
| 프로덕션 승격 | **PR #33** → merge `d773fbf` | Actions **success** (06:09 UTC) |

프로세스 준수: Staging 선반영 → 사용자 확인 → 승인 후 PR 병합 → 프로덕션. 직접 main push 없음.

---

## 남은 할 일 (이월)

### 바로 처리 가능 — 상표 표기 2군데 (사용자 판단 대기)
같은 `Gyrokinesis®` 표기가 아직 남아 있음. Instructor 칩이 아닌 별도 맥락이라 이번엔 손대지 않음.
1. `pilates-landing/index.html` JSON-LD `knowsAbout` — `"Gyrokinesis®"` (SEO 구조화 데이터)
2. `translations.ts` 후기 섹션 `Gyrokinesis® mat / 1 year` — **현재 숨김 상태**인 Testimonials
   (참고: About 섹션 `AboutSection.tsx`는 이미 `GYROKINESIS®`로 정상)

### 기존 이월분
- Cloudflare Pages 이전 검토 (소스 private + 호스팅 은닉 동시 달성). `/staging` 구조 처리 방식 결정 선행.
  ※ 무료 플랜에서 "저장소 private + GitHub Pages"는 불가 — 07-23 사고 기록 참조.
- Google Search Console + Google Business Profile 등록 (사용자 작업).
- FUTURE-PLANS.md: 신청자 상태 관리, 대기자 명단, 수강생 포털/VOD (Gyrotonic 본부 정책 확인 선행).
